/**
 * Re-exports Zustand store hooks for convenient access.
 * Import from this module rather than importing stores directly.
 */

export { useInventoryStore, BLOCK_COLORS } from '../store/inventoryStore.js';
